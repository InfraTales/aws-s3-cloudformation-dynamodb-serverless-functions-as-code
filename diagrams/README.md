# Diagrams

- `architecture_model.json` is the normalized source of truth extracted from the promoted repo.
- `rendered/repo-architecture.png` is the fuller GitHub-facing technical diagram.
- `rendered/article-architecture.png` is the simplified editorial image used inside the Ghost article.

Treat `diagram-manifest.json` as the source of truth for which assets are approved for article placement.
